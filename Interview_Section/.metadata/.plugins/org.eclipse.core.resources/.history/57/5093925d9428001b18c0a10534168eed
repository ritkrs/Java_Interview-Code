(1)  IDENTITYHASHMAP:-(i)It is exactly same as HashMap except the following differences. 
     (ii) In the case of HashMap JVM will always use “.equals()”method to identify duplicate keys. 
	 (iii)But in the case of IdentityHashMap JVM will use== (double equal operator) to identify duplicate keys. 