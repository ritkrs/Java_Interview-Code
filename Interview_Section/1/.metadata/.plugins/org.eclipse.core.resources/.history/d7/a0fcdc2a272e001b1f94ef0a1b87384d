package java8;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

public class Hello {
	public static void main(String[] args) {
		ArrayList a1=new ArrayList();
		for (int i = 0; i<=10; i++) 
			if(i%2==0)
				a1.add(i);
		System.out.println(a1);
		//by using java 8.
		ArrayList a2=new ArrayList();
		for (int i = 0; i <=20; i++) {
			a2.add(i);
		}
		System.out.println(a2);
		//List l2=a1.stream().filter(a2 -> a2%2==0).collect(Collectors.toList());
		//List<Integer> l3 = a1.stream().filter(i -> i%2==0).collect(Collectors.toList());
		
		
		
	}

}
